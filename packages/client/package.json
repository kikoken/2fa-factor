{
  "name": "@2fa-factor/client",
  "version": "1.0.0",
  "description": "Client-side 2FA integration library",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["2fa", "client", "browser", "integration"],
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@2fa-factor/core": "workspace:*"
  },
  "publishConfig": {
    "access": "restricted"
  }
}